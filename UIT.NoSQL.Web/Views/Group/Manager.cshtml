@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@if (ViewBag.Member == true)
{
   
    @section LeftMenu {
        <section id="leftmenu">
            @Html.Action("LeftMenu", "Home")
        </section>
    }
    
    {
    ViewBag.Title = "User setting";
    }

    @Html.Action("ManagerUser", "Group")
}
else
{
    {
        ViewBag.Title = "Manager";
    }
    
    @section LeftMenu {
        <section id="leftmenu">
            @Html.Action("LeftManager", "Group")
        </section>
    }

    <div id="MyContent"></div>
}
    @section menu {
        <section id="topmenu">
            @Html.Action("TopMenuUser", "Group", new{action = "Manage"})
        </section>
    }
